
# Install


### Rust
```
curl https://sh.rustup.rs -sSf | sh
```

### System dependencies
```
sudo apt update
sudo apt install -y libopencv-dev pkg-config clang llvm-dev libclang-dev
```

# Run
use libcamerify command to run

### TODO
- create config to store variables


